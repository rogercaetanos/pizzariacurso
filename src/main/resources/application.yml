spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=bd_pizzaria_curso;integratedSecurity=false;encrypt=false;trustServerCertificate=true
    username: sa
    password: '@ITB123456'
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true # Isso mostra as consultas SQL no console para fins de debug

